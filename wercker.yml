box: golang
build:
  steps:
    - arjen/hugo-build:
        theme: hugo-hikari-theme
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
