box: golang
build:
  steps:
    - arjen/hugo-build:
        theme: hugo-hikari-theme
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - iflowfor8hours/gh-pages:
        token: $GIT_TOKEN
        domain: iflowfor8hours.github.io/iflowfor8hours.info
        basedir: public
