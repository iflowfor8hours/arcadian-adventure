box: debian
build:
  steps:
    - arjen/hugo-build@1.9.2
        version: "0.15"
        theme: hugo-hikari-theme
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
